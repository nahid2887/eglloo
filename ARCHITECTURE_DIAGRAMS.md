# Visual Architecture & Flow Diagrams

## System Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                         INTERNET (Users)                       │
└────────────────────────┬───────────────────────────────────────┘
                         │
                         │ HTTPS (Port 443)
                         │ HTTP (Port 80)
                         │
        ┌────────────────▼─────────────────┐
        │                                  │
        │     🔐 NGINX REVERSE PROXY       │
        │                                  │
        │  • SSL/TLS Encryption           │
        │  • HTTP → HTTPS Redirect        │
        │  • Request Routing              │
        │  • Compression (GZIP)           │
        │  • Security Headers             │
        │  • Rate Limiting                │
        │                                  │
        └────────────────┬─────────────────┘
                         │
                         │ HTTP (Port 8005)
                         │ Internal Network
                         │
        ┌────────────────▼─────────────────────────────────────┐
        │                                                      │
        │          🐍 DJANGO APPLICATION (web)               │
        │                                                      │
        │  Routes:                                            │
        │  • /api/              → REST API endpoints          │
        │  • /admin/            → Django admin panel          │
        │  • /swagger/          → API documentation           │
        │  • /static/           → CSS, JS, images             │
        │  • /media/            → User uploads                │
        │  • /health            → Status check                │
        │                                                      │
        └────────────────┬─────────────────────────────────────┘
                         │
                         │ TCP Connection
                         │ Port 5432
                         │
        ┌────────────────▼──────────────────┐
        │                                   │
        │   🗄️  POSTGRESQL DATABASE         │
        │                                   │
        │  • Data Storage                  │
        │  • User Management               │
        │  • Projects, Tasks, Estimates    │
        │  • Timesheets                    │
        │                                   │
        └───────────────────────────────────┘
```

---

## Request Flow - Example: Access Swagger Docs

```
User in Browser
│
├─ Type: https://app.lignaflow.com/swagger/
│
▼
Internet
│
▼
┌─────────────────────────────────────────┐
│ Nginx Server (Port 443 - HTTPS)        │
│                                         │
│ 1. Receive request                     │
│ 2. Check SSL certificate               │
│ 3. Decrypt HTTPS traffic               │
│ 4. Route /swagger/ → Django             │
│ 5. Add security headers                │
└────────────┬────────────────────────────┘
             │
             ▼ HTTP (Internal, Port 8005)
             │
┌────────────────────────────────────────────┐
│ Django App                                 │
│                                            │
│ 1. Receive /swagger/ request              │
│ 2. Load Swagger UI                        │
│ 3. Generate API documentation             │
│ 4. Return HTML + CSS + JS                 │
└────────────┬────────────────────────────────┘
             │
             ▼ HTTP Response
             │
┌────────────────────────────────────────────┐
│ Nginx Server (Port 443 - HTTPS)           │
│                                            │
│ 1. Receive response from Django           │
│ 2. Add security headers                   │
│ 3. Compress with GZIP                     │
│ 4. Encrypt with SSL/TLS                   │
│ 5. Send back to client                    │
└────────────┬────────────────────────────────┘
             │
             ▼ HTTPS
             │
    User sees Swagger UI
    in browser!
```

---

## Deployment Stages

### Stage 1: Local Development
```
Your Computer
└── Git Repository
└── Docker Compose (up -d)
    ├── Nginx Container
    ├── Django Container
    └── PostgreSQL Container

Access: https://localhost/swagger/
```

### Stage 2: Push to GitHub
```
Your Computer                GitHub
│                               │
├─ git add .                   │
├─ git commit -m "..."         │
└─ git push origin develop ───→ Repository

Benefits:
• Backup of code
• Team collaboration
• Deployment automation
• Version history
```

### Stage 3: Deploy to Server
```
Your Server (cloud.provider.com, app.lignaflow.com)
│
├─ git clone from GitHub
├─ Generate SSL certificates (Let's Encrypt)
├─ docker-compose build
├─ docker-compose up -d
│   ├── Nginx (Port 80, 443)
│   ├── Django (Port 8005)
│   └── PostgreSQL (Port 5432)
│
└─ Configure DNS to point app.lignaflow.com → Server IP

Access: https://app.lignaflow.com/swagger/
```

---

## Docker Services Interaction

```
┌─────────────────────────────────────────────────┐
│           Docker Compose Network                │
│       (eagleeyeau-network - bridge)             │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌───────┐ │
│  │              │  │              │  │       │ │
│  │  nginx       │  │  web         │  │  db   │ │
│  │  Container   │  │  Container   │  │       │ │
│  │              │  │              │  │       │ │
│  │ Port 80/443  │──│ Port 8005   │──│ 5432  │ │
│  │              │  │              │  │       │ │
│  └──────────────┘  └──────────────┘  └───────┘ │
│        │                  │              │      │
│        └──────────────────┴──────────────┘      │
│              Internal Communication            │
│              (via network aliases)             │
│                                                 │
│  Can access each other by service name:        │
│  • nginx can reach "web" at web:8005          │
│  • web can reach "db" at db:5432              │
└─────────────────────────────────────────────────┘
```

---

## Git Workflow

```
┌───────────────────────────────────────────┐
│      Your Local Computer (Feature Branch) │
│                                           │
│  1. Create branch: git checkout -b feat  │
│  2. Make changes                         │
│  3. git add .                            │
│  4. git commit -m "message"              │
└───────────────┬───────────────────────────┘
                │
                │ git push origin feature
                │
        ┌───────▼────────┐
        │                │
        │  GitHub.com    │
        │  Repository    │
        │                │
        └───────┬────────┘
                │
        ┌───────▼────────┐
        │                │
        │  Create Pull   │
        │  Request (PR)  │
        │  feature→dev   │
        │                │
        └───────┬────────┘
                │
        ┌───────▼────────┐
        │                │
        │  Review Code   │
        │  Tests Pass    │
        │                │
        └───────┬────────┘
                │
        ┌───────▼────────┐
        │                │
        │  Merge PR      │
        │  to develop    │
        │                │
        └───────┬────────┘
                │
        ┌───────▼────────┐
        │                │
        │  Pull latest   │
        │  git pull      │
        │  origin dev    │
        │                │
        └───────┬────────┘
                │
        ┌───────▼────────────────────┐
        │                            │
        │ Your Local (updated)       │
        │ Ready for next feature     │
        │                            │
        └────────────────────────────┘
```

---

## SSL/TLS Encryption Flow

```
┌──────────────────────────────────┐
│ User's Browser                   │
│                                  │
│ Request: HTTPS                   │
│ URL: https://app.lignaflow.com   │
└──────────────┬───────────────────┘
               │
               │ TCP Handshake
               │
        ┌──────▼──────────────────┐
        │ TLS/SSL Negotiation      │
        │                          │
        │ 1. Client Hello          │
        │ 2. Server Cert           │
        │ 3. Key Exchange          │
        │ 4. Session Keys Generated│
        │                          │
        └──────┬──────────────────┘
               │
        ┌──────▼──────────────────┐
        │ Encrypted Connection    │
        │                          │
        │ All data encrypted with  │
        │ AES-256 (or similar)     │
        │                          │
        └──────┬──────────────────┘
               │
        ┌──────▼──────────────────┐
        │ Nginx Server             │
        │                          │
        │ Decrypt request          │
        │ Process                  │
        │ Encrypt response         │
        │                          │
        └──────┬──────────────────┘
               │
        ┌──────▼──────────────────┐
        │ Safe Encrypted Channel   │
        │                          │
        │ User data protected      │
        │ Attacker can't see:      │
        │ • Password              │
        │ • API responses         │
        │ • Session tokens        │
        │ • Personal data         │
        │                          │
        └──────────────────────────┘
```

---

## Environment Variables Flow

```
┌──────────────────┐
│ .env.example     │ Template with defaults
│ (in Git)         │ NO secrets here!
└────────┬─────────┘
         │
         │ Copy and customize
         │
    ┌────▼────────────┐
    │ .env file        │ Your actual secrets
    │ (NOT in Git)     │ Database password, keys, etc.
    └────┬────────────┘
         │
         │ Load by Docker
         │
    ┌────▼────────────────────────────┐
    │ docker-compose.yml              │
    │                                 │
    │ environment:                    │
    │  - DATABASE_URL from .env       │
    │  - SECRET_KEY from .env         │
    │  - ALLOWED_HOSTS from .env      │
    │                                 │
    └────┬─────────────────────────────┘
         │
    ┌────▼────────────────────────────┐
    │ Django Container                │
    │                                 │
    │ Access settings from environ:   │
    │ os.environ.get('DATABASE_URL')  │
    │                                 │
    └─────────────────────────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────────┐
│ Internet (Untrusted)                    │
└────────────────┬────────────────────────┘
                 │
        ┌────────▼─────────┐
        │ Layer 1: HTTPS   │
        │ (SSL/TLS)        │
        │ Encryption       │
        └────────┬─────────┘
                 │
        ┌────────▼──────────────────┐
        │ Layer 2: Security Headers  │
        │ • HSTS                    │
        │ • X-Frame-Options         │
        │ • X-Content-Type-Options  │
        │ • X-XSS-Protection        │
        └────────┬──────────────────┘
                 │
        ┌────────▼──────────────────┐
        │ Layer 3: Authentication   │
        │ • Django auth             │
        │ • API tokens              │
        │ • Session management      │
        └────────┬──────────────────┘
                 │
        ┌────────▼──────────────────┐
        │ Layer 4: Authorization    │
        │ • Permission checks       │
        │ • Role-based access       │
        │ • Data isolation          │
        └────────┬──────────────────┘
                 │
        ┌────────▼──────────────────┐
        │ Layer 5: Database         │
        │ • Prepared statements     │
        │ • SQL injection prevention│
        │ • Encrypted passwords     │
        └────────────────────────────┘
```

---

## Monitoring & Logs Flow

```
┌────────────────────────────────────┐
│ All Services Generate Logs         │
└────────────────────────────────────┘
         │          │          │
    ┌────▼─┐   ┌───▼──┐  ┌───▼──┐
    │Nginx │   │Django│  │  DB  │
    │logs  │   │logs  │  │ logs │
    └────┬─┘   └───┬──┘  └───┬──┘
         │         │         │
         └────┬────┴────┬────┘
              │         │
        ┌─────▼─────────▼────────┐
        │ Docker Compose          │
        │ Log Aggregation         │
        │                         │
        │ docker-compose logs -f  │
        │ Shows all service logs  │
        │ in real-time            │
        └─────┬────────────────────┘
              │
        ┌─────▼─────────────────┐
        │ Log Analysis           │
        │                        │
        │ grep, tail, awk, etc.  │
        │ Find errors & issues   │
        │                        │
        └────────────────────────┘
```

---

## Deployment Timeline

```
Day 1: Setup
┌──────────────────────────────────┐
│ bash setup-ssl.sh                │
│ docker-compose build             │
│ docker-compose up -d             │
│ Test: curl -k https://localhost  │
└──────────────────────────────────┘

Day 2: Version Control
┌──────────────────────────────────┐
│ git init                         │
│ git add .                        │
│ git commit -m "Initial commit"   │
│ git push origin main             │
└──────────────────────────────────┘

Day 3: Deploy to Server
┌──────────────────────────────────┐
│ SSH to server                    │
│ git clone ...                    │
│ bash setup-ssl.sh (Let's Encrypt)│
│ docker-compose up -d             │
│ Configure DNS                    │
│ Test: https://app.lignaflow.com  │
└──────────────────────────────────┘

Day 4+: Maintenance
┌──────────────────────────────────┐
│ Monitor logs                     │
│ Check certificates               │
│ Update code regularly            │
│ Backup database                  │
│ Scale services if needed         │
└──────────────────────────────────┘
```

---

## Key Points

✅ **Nginx** = Web server handling HTTP/HTTPS
✅ **SSL/TLS** = Encryption for app.lignaflow.com
✅ **Docker** = Containers for isolation and consistency
✅ **Git** = Version control and collaboration
✅ **Swagger** = API documentation at /swagger/
✅ **Django** = Application logic
✅ **PostgreSQL** = Data persistence
